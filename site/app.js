const $=s=>document.querySelector(s);let data,docs;
async function init(){data=await fetch('/api/docs').then(r=>r.json());docs=new Map(data.docs.map(d=>[d.id,d]));renderNav();window.addEventListener('hashchange',load);$('#search').addEventListener('input',search);load()}
function renderNav(){let h='<div class="group"><div class="sidebar-title">开始阅读</div>'+[['home','总览'],['roadmap','学习路线'],['design','项目设计']].map(x=>`<a class="side-link" href="#${x[0]}">${x[1]}</a>`).join('')+'</div><div class="group"><div class="sidebar-title">协议模块</div>';for(const [id,title] of data.modules){h+=`<div class="module-name">${title}</div><div class="subnav">${[['index','概览'],['protocol','协议设计'],['state-machine','状态机'],['source','源码导航'],['references','参考资料']].map(x=>`<a class="side-link" href="#${id}/${x[0]}">${x[1]}</a>`).join('')}</div>`}$('#sidebar').innerHTML=h+'</div>'}
async function load(){const rawId=location.hash.slice(1)||'home';if(rawId.startsWith('source-file/'))return loadSource(decodeURIComponent(rawId.slice(12)));const id=rawId;const d=docs.get(id)||docs.get('home');document.title=`${d.title} · Network Protocol`;$('#crumb').textContent=d.module?`${d.module} / ${d.kind}`:'NETWORK PROTOCOL';$('#content').innerHTML='<div class="loading">载入中…</div>';const body=await fetch('/api/doc?id='+encodeURIComponent(d.id)).then(r=>r.json());$('#content').innerHTML=body.html;if(d.kind==='source'){const files=await fetch('/api/source-files?module='+d.module).then(r=>r.json());$('#content').insertAdjacentHTML('beforeend',`<section class="source-index"><h2>上游源码文件</h2><p>以下文件保留上游目录结构，点击文件名即可在网页中阅读。</p>${files.files.map(f=>`<a class="source-file" href="#source-file/${encodeURIComponent(f)}"><span>${f.split('/').pop()}</span><small>${f}</small></a>`).join('')}</section>`)}document.querySelectorAll('.side-link').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+d.id));makeToc();window.scrollTo(0,0)}
async function loadSource(file){document.title=`${file.split('/').pop()} · Network Protocol`;$('#crumb').textContent=`源码 / ${file}`;$('#content').innerHTML='<div class="loading">载入中…</div>';const body=await fetch('/api/source?file='+encodeURIComponent(file)).then(r=>r.json());const lines=body.content.split('\n');$('#content').innerHTML=`<h1>${file.split('/').pop()}</h1><p class="source-path">${file}</p><pre class="source-view">${lines.map((line,i)=>`<span id="L${i+1}" class="line"><a href="#source-file/${encodeURIComponent(file)}#L${i+1}">${String(i+1).padStart(4,' ')}</a> ${escapeHtml(line)}</span>`).join('\n')}</pre>`;$('#toc').innerHTML='<div class="toc-title">源码阅读</div><a href="#'+file.split('/')[0]+'/source">返回源码导航</a>';window.scrollTo(0,0)}
function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function makeToc(){const hs=[...document.querySelectorAll('article h2,article h3')];$('#toc').innerHTML=hs.length?'<div class="toc-title">本页目录</div>'+hs.map(h=>`<a href="#${h.id}">${h.textContent}</a>`).join(''):'<div class="empty">长文阅读<br>从左侧选择章节</div>'}
function search(e){const q=e.target.value.trim().toLowerCase();document.querySelector('.results')?.remove();if(!q)return;const box=document.createElement('div');box.className='results';const hits=data.docs.filter(d=>(d.title+' '+d.id).toLowerCase().includes(q)).slice(0,12);box.innerHTML=hits.length?hits.map(d=>`<a href="#${d.id}">${d.title}<small>${d.file}</small></a>`).join(''):'<div class="empty" style="padding:13px">没有匹配的文档</div>';$('.top').append(box)}init();
