# 间章 · NDP 在哪里、双栈上网经历了什么

> 本章不属于任何单个模块，而是把 `arp-ndp`、`dhcp`、`ipv4-ipv6`、`icmp` 串成一条「用户从插上网线到打开网页」的完整路线。先说明 **NDP 到底覆盖了什么、替代了 IPv4 的哪些协议**，再从**家庭网络、校园网、云服务网络**三种真实环境出发，对比同一件事在 IPv4 与 IPv6 下的做法。

## 为什么需要这一章

`arp-ndp` 模块容易给人一个误解：**NDP 只是「IPv6 版的 ARP」**。实际上 ARP 只做一件事——IP 到 MAC 的地址解析；而 NDP 把 IPv4 世界里由**多个互不相关协议**拼起来的一整套链路内功能，统一进了 ICMPv6 消息族。阅读单个模块时，这些职责被打散在 DHCP、ICMP、路由、SLAAC 各处，只有在「一次真实上网」里才会同时出现。

本章回答两个问题：

1. NDP 覆盖的职责集合是什么，它分别取代或整合了 IPv4 的哪些机制；
2. 一个普通用户上网，报文在家庭、校园、云三种网络里分别经过哪些协议与状态变化，双栈（IPv4 + IPv6）如何并存与选择。

## 一、NDP 覆盖了什么，取代了 IPv4 的什么

NDP 不是一个协议，而是 **RFC 4861 定义的一组 ICMPv6 消息**。把它拆开，与 IPv4 世界的对应关系如下：

| NDP 消息（ICMPv6 type） | 承担的功能 | IPv4 里的对应物 | 差异要点 |
| --- | --- | --- | --- |
| RS (133) / RA (134) | 路由器发现、默认网关、前缀发现 | ICMP Router Discovery（RFC 1256，**几乎无人部署**）/ DHCP option 3 | IPv6 默认可用；网关与 DNS 都能从 RA 直接得到 |
| NS (135) / NA (136) | 地址解析（IP → MAC） | ARP（RFC 826） | 用 solicited-node 多播替代广播 |
| NS / NA（源 `::`） | 重复地址检测（DAD） | 无标准；靠免费 ARP、DHCP 探测、RFC 5227 ACD | DAD 是地址启用前的**强制**步骤 |
| Redirect (137) | 更正下一跳 | ICMP Redirect（RFC 792 type 5） | 同为 ICMP 承载，语义类似 |
| Prefix Information option | 无状态地址自动配置（SLAAC） | DHCPv4 分配地址（或手工） | 主机自己拼地址，服务器只发前缀 |
| MTU option | 链路 MTU、PMTU | ICMP Fragmentation Needed / PMTUD（RFC 1191） | IPv6 强制最小 MTU 1280，路由器不分片 |
| RDNSS option（RFC 8106） | 下发 DNS 服务器 | DHCP option 6 | IPv6 可完全不用 DHCP 就拿到 DNS |
| 邻居状态机（NUD） | 可达性探测与失效发现 | 无（ARP 仅靠「被使用」被动刷新） | IPv6 主动探测，失败即回收 |
| — | 组播成员管理 | IGMP | IPv6 里对应 MLD，与 NDP 依赖的多播配套 |

一句话概括两者的设计哲学：

- **IPv4**：地址解析（ARP）、路由发现（ICMP RD）、地址配置（DHCP）、冲突检测（免费 ARP/ACD）是**四套独立协议**，各自演进、各自有历史包袱，且都基于广播。
- **IPv6**：把这些统一到 **ICMPv6 + 多播 + 强制 DAD** 上；地址配置默认走 **SLAAC**，DHCPv6 退居「补充参数」或「需要集中管控时」的可选项。

因此「IPv6 还需要 ARP 吗」的答案是**不需要**——NDP 是它的替代设计，而不是补充。IPv6 甚至取消了链路层广播的概念，全部用多播表达「通告、请求、找谁」。

## 二、上网前，本机必须先具备什么

无论哪种网络，主机要能发出第一个去往外部世界的报文，必须先拿到四样东西：

1. **一个可用的 IP 地址 + 前缀长度**；
2. **默认网关**（下一跳）；
3. **DNS 服务器**；
4. **地址与映射的合法性**（不能和邻居冲突）。

双栈下这四样东西**各拿两份**（一份 IPv4、一份 IPv6），而且获取方式在两种协议下完全不同：

| 需要的东西 | IPv4 获取方式 | IPv6 获取方式 |
| --- | --- | --- |
| 地址 + 前缀 | DHCPv4（DORA）或手工 | SLAAC（RA 前缀 + 接口标识）或 DHCPv6 有状态 |
| 默认网关 | DHCP option 3 | RA 的 Router Lifetime + 路由器地址 |
| DNS | DHCP option 6 | RA 的 RDNSS option，或 DHCPv6 无状态 |
| 冲突检测 | 免费 ARP / DHCP 探测（可选） | DAD（强制，地址先 tentative） |
| 地址到 MAC | ARP（广播） | NS/NA（solicited-node 多播） |

注意最后两行：**即使 IPv4 用 DHCP 拿到地址，邻居解析仍然要靠 ARP**；而 IPv6 里地址配置（SLAAC）和邻居解析（NDP）本就是同一套报文体系。

## 三、一次上网的通用流程

不管下面哪种网络，用户点开 `https://example.com` 后，内核大致按这个顺序工作：

```text
应用（浏览器）
→ DNS 解析 example.com                 # 拿到目的 IP（可能同时得到 A 和 AAAA）
→ 选路：目的是否在 on-link 前缀内？不在 → 用默认网关
→ 邻居解析：网关 IP → 网关 MAC         # IPv4: ARP；IPv6: NS/NA
→ 封装：TCP → IP → Ethernet            # 合成帧，目的 MAC = 网关
→ 第一跳（网关）→ ISP / 骨干 → 目的     # 逐跳重复「选路 + 邻居解析」
→ 到达服务器；返回路径同理
```

其中「邻居解析」正是 `arp-ndp` 模块的闭环，「选路」是 `ipv4-ipv6` / `routing` 模块，「TCP + TLS + HTTP」是上层模块。后面三种网络的区别，主要集中在**第一跳之前**（谁给地址）、**第一跳是什么**（真路由器还是虚拟交换机）、以及**有没有 NAT**。

## 四、家庭网络

典型拓扑：

```text
运营商 ⇄ 光猫(ONT) ⇄ 家用路由器 ⇄ 交换机/AP ⇄ 手机、笔记本、电视
                    （NAT + DHCPv4 Server + 可选 RA）
```

### IPv4 路径

1. 手机连上 WiFi，还没有地址，广播 **DHCP DISCOVER**（`0.0.0.0:68` → `255.255.255.255:67`）。
2. 家用路由器作为 DHCP 服务器回 **OFFER**、收 **REQUEST**、发 **ACK**，于是手机拿到 `192.168.1.0/24` 段的一个地址、网关 `192.168.1.1`、DNS（通常也指向路由器）。
3. 上外网时，手机把包发给网关。解析 `192.168.1.1` 的 MAC → **ARP 广播**（全链所有设备都要看一眼）。
4. 路由器做 **NAT/PAT**：把源地址 `192.168.1.x:port` 改写成运营商分配的公网地址 + 一个端口号，并记入 conntrack 表。
5. DNS 查询走 `UDP 53`（通常发给路由器，由它转发/缓存）；网页走 `TCP 443` + TLS。

要点：**家庭 IPv4 的「第一跳」是 NAT 路由器**；NAT 让内网设备共享一个公网地址，代价是外部无法主动连入、端到端语义被破坏。

### IPv6 路径（若 ISP 与路由器支持）

1. 家用路由器通过 PPPoE 或 **DHCPv6-PD（前缀委派，RFC 8415）** 从 ISP 拿到一段前缀，例如 `2001:db8:abcd::/56`，再把其中一个 `/64` 分给 LAN。
2. 路由器周期性发 **RA**，带两个关键信息：
   - **Prefix Information**：前缀 `2001:db8:abcd:1::/64`，`A`（Autonomous）标志置位；
   - **RDNSS**：DNS 服务器地址。
3. 手机收到 RA 后走 **SLAAC**：用「前缀 + 接口标识」自己拼出全球地址（EUI-64，或按 RFC 8981 隐私扩展随机生成、定期更换），先做 **DAD** 确认不冲突，再启用。
4. 默认网关直接来自 **RA**，不需要 DHCP 参与。
5. 上外网时解析网关 MAC → **NS/NA（solicited-node 多播）**，只惊动「最可能持有该地址」的设备，而不是全链广播。
6. 理想情况下**没有 NAT**：每个设备有全球可路由地址，端到端可达（家庭防火墙仍会默认拦截入站）。

### 双栈如何选择

- DNS 同时返回 **A（IPv4）** 与 **AAAA（IPv6）** 记录；
- 浏览器用 **Happy Eyeballs（RFC 8305）**：并发尝试 IPv6 与 IPv4，谁先握手成功用谁，避免「有 AAAA 但 IPv6 不通」时长时间卡住；
- 纯 IPv6 网络访问 IPv4 资源时，靠 **NAT64 / DNS64** 或 **464XLAT** 兜底。

## 五、校园网

典型拓扑：

```text
宿舍/教室终端 → 接入交换机 → 汇聚/核心 → 出口网关 → CERNET / 运营商
                     （VLAN 隔离、802.1X / Portal 认证、ACL、计费）
```

校园网的特点是**接入即认证、二层是大交换网络、出口策略复杂**：

- **IPv4**：
  - 认证先行——**802.1X**、Web Portal 或专用客户端（DrCOM 等）通过后才放行；认证与 DHCP 时序耦合，未认证时可能连地址都拿不到。
  - DHCP 分配校园私网（甚至公网）地址段，按楼栋/房间划分 **VLAN** 与地址池。
  - DNS 由校方 DHCP 下发（校内域名解析到内网地址），出口再做一层 NAT 或直接路由。
  - 由于是交换网络，**ARP 泛洪被限制在 VLAN 内**；跨楼栋通信由核心三层转发，每跨一个三层边界都要重新做一次邻居解析。
- **IPv6**：
  - 校园网往往是最早、最积极部署 IPv6 的场景（CERNET2 等）；常见 **SLAAC + RA**，也可能用 DHCPv6 有状态做集中管理。
  - RA 携带校园前缀与 RDNSS；出口 ACL 决定哪些 IPv6 资源可达，很多学校「IPv6 免流量费」。
  - 跨子网漫游（从宿舍走到教学楼）会换前缀，主机重新 SLAAC + **DAD**，旧地址进入 deprecated 状态并逐步回收。

要点：校园网把**认证、VLAN 隔离、计费**压在数据路径上，是「同一个协议、不同运维策略」最明显的例子；IPv6 在这里通常是「与 IPv4 并行的一套前缀 + 一套 DNS」，而非替代。

## 六、云服务网络

典型拓扑：

```text
云主机(实例) → 虚拟交换机/Overlay(VXLAN/GENEVE) → VPC 子网 → 网关/NAT/负载均衡 → 公网
```

云上「上网」与家庭/校园最大的不同：**邻居和网关大多是虚拟的，地址配置往往由元数据/API 驱动，而不是广播式 DHCP**。

- **实例接入网络的方式**：
  - 云厂商通常仍提供一个「本地 DHCP」给实例下发私网地址、`169.254.169.254` 元数据服务地址和路由；但核心配置也可能通过 **cloud-init + DHCP option 121（无类静态路由）**、config-drive、API 注入完成。
  - `169.254.169.254`（AWS IMDS 等）是一个链路本地特殊地址，实例用普通 IP 路径访问它拿元数据——这是「引导信息」在云上的一种替代形态。
- **IPv4**：
  - VPC 内是私网地址，出公网走 **NAT 网关**或绑定 **弹性 IP（EIP）**；
  - 负载均衡、安全组、ACL 在虚拟交换机/网关处生效，效果上类似「可编程的路由器 + 防火墙」。
- **IPv6**：
  - VPC 与子网可关联 IPv6 CIDR，实例获得全球 IPv6 地址，出网通常**不需要 NAT**（但仍受安全组约束）；
  - 邻居发现发生在宿主机虚拟交换机/Overlay 上，**ARP/NDP 表规模巨大**，且受软件转发性能约束。
- **Overlay 的影响**：
  - 数据包在宿主机上被 **VXLAN/GENEVE 封装**，真实二层是物理数据中心网络，租户看到的是「虚拟二层」；因此 ARP/NDP 发生在虚拟网卡与虚拟交换机之间，与物理拓扑解耦。

要点：云网络把「地址分配、邻居、网关、NAT、ACL」都做成了**软件定义、API 驱动**；DHCP/ARP/NDP 依然存在，但退化为控制面下发结果在数据面的体现。

## 七、三场景对照

| 维度 | 家庭网络 | 校园网 | 云服务网络 |
| --- | --- | --- | --- |
| 第一跳 | 家用 NAT 路由器 | 接入交换机 + 认证网关 | 虚拟交换机 / Overlay |
| IPv4 地址来源 | DHCPv4（DORA） | DHCPv4（+ 认证） | 本地 DHCP / 元数据 / API |
| IPv6 地址来源 | SLAAC（RA + PD 前缀） | SLAAC 或 DHCPv6 有状态 | VPC CIDR + SLAAC/DHCPv6 |
| 默认网关 | DHCP option 3 | DHCP / RA | DHCP option 121 / RA |
| DNS | DHCP option 6 | SOP 下发 DNS | 元数据 / DHCP / RA RDNSS |
| 有没有 NAT | 有（PAT） | 常见（出口 NAT） | IPv4 有，IPv6 通常没有 |
| 邻居解析 | ARP / NS-NA（广播/多播） | VLAN 内 ARP / NS-NA | 虚拟交换机上的 ARP / NDP |
| 二层范围 | 一个家庭广播域 | 大量 VLAN，三级转发 | Overlay 虚拟二层 |
| 典型痛点 | IPv6 依赖 ISP/路由器 | 认证与计费、跨子网漫游 | 表规模、软件转发性能 |

## 八、把流程放回分层

| 步骤 | 层次 | 涉及模块 |
| --- | --- | --- |
| 拿到地址/网关/DNS | L3 配置（应用层封装） | `dhcp`、`ipv4-ipv6`；IPv6 走 NDP/RA |
| 判断目的是否 on-link、选网关 | L3 路由 | `ipv4-ipv6`、`routing` |
| 目的 IP → MAC | L2.5 邻居 | `arp-ndp` |
| 合成以太网帧、发第一跳 | L2 | `ethernet`、`vlan-bridge` |
| 跨网段转发、NAT | L3 | `ipv4-ipv6`、`routing`（云上还有 overlay） |
| 差错与 PMTU 反馈 | L3 | `icmp` |
| 建立连接、传输 | L4 | `tcp`、`udp` |
| 名称解析 | L7 | `dns` |
| 加密与网页 | L5–L7 | `tls`、`http1-1` / `http2` / `quic-http3` |

## 九、常见困惑

- **「有了 IPv6 还需要 ARP 吗？」** 不需要；ARP 不承载于 IPv6，NDP 完全替代它，并顺带接管了路由发现、DAD、重定向。
- **「IPv6 还有 DHCP 吗？」** 有，但默认分工不同：SLAAC 负责地址，DHCPv6 无状态负责参数（如没有 RDNSS 时下发 DNS），有状态 DHCPv6 用于需要集中管控的场景。
- **「为什么 IPv6 没有广播？」** 设计上取消广播，用多播（solicited-node、all-nodes、all-routers）表达所有「通告/询问」，降低无关节点的负担。
- **「为什么 IPv6 路由器不分片？」** 路由器不做分片，靠 **PMTUD** 和**强制最小 MTU 1280**；这避免了 IPv4 里 DF 位造成的分片黑洞（参见 `arp-ndp` 的 PTB 处理与 `icmp` 模块）。
- **「IPv6 里没有 NAT 了吧？」** 原则上每个设备有全球地址、不做 NAT；但防火墙、NPTv6，以及纯 IPv6 网络访问 IPv4 时的 NAT64 仍然存在。
- **「地址一换就要重新 DAD，不会很慢吗？」** DAD 只需一次多播往返，且只针对 tentative 地址；优化手段（DAD 次数、乐观 DAD RFC 4429）可进一步降低时延。

## 十、继续阅读

- 邻居解析的报文与状态机：`arp-ndp/protocol.md`、`arp-ndp/state-machine.md`
- 地址租约与 DORA：`dhcp/protocol.md`
- IPv6 地址配置、分片与 PMTU：`ipv4-ipv6/protocol.md`
- 差错与 PTB：`icmp/protocol.md`
- 路由与转发：`routing/README.md`
